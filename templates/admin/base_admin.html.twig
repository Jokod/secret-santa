{% extends 'base.html.twig' %}

{% block nav %}
    <a href="{{ path('admin_dashboard') }}">Tableau de bord</a>
    <a href="{{ path('admin_participants') }}">Participants</a>
    <a href="{{ path('admin_exclusions') }}">Exclusions</a>
    <a href="{{ path('admin_settings') }}">Configuration</a>
    <a href="{{ path('admin_draw') }}">Tirage</a>
    <a href="{{ path('admin_messages') }}">Messages</a>
    <form class="logout-form" action="{{ path('admin_logout') }}" method="post">
        <input type="hidden" name="_csrf_token" value="{{ csrf_token('logout') }}">
        <button class="btn-logout" type="submit">Déconnexion</button>
    </form>
{% endblock %}
